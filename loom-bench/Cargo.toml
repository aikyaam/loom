[package]
name = "loom-bench"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Loom benchmark harness"

[[bench]]
name = "codec_bench"
harness = false

[dependencies]
loom-core = { path = "../loom-core" }
flacenc = "0.5"
fastrand = "2"
hound = "3.5"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
claxon = "0.4"
